(define (call f)
  (let* ([res1 (apply + (list 1 2 3 4))]
         [res2 (apply f (list 10 20))]
         [multiplication *] ; aliasing builtin define-prim '*
         [res3 (apply multiplication (list res1 res2))])

    (list res1 res2 res3)))

(define (brouhaha_main)
  (call +))

